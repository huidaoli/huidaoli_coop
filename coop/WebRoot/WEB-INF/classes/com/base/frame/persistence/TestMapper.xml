<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.base.frame.persistence.TestMapper">

  <cache />

  <select id="getTestByname"  parameterType="string" resultType="Test">
    SELECT
         ID,NAME,ADDRESS
    FROM TEST where name = #{username}
  </select>
  
  <insert id="insertTest" parameterType="Test">
    INSERT INTO TEST (id, name, address)
     VALUES (#{id}, #{name}, #{address})
  </insert>


</mapper>