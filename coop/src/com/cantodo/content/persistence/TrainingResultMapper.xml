<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cantodo.content.persistence.TrainingResultMapper">


	<resultMap type="com.cantodo.content.dto.TrainingResult"
		id="trainingResult">
		<id property="id" column="id" />
		<result property="name" column="name" />
		<result property="type" column="type" />
		<result property="createDate" column="createDate" />
		<result property="content" column="content" />
		<result property="contcode" column="contcode" />
	</resultMap>
	
	<resultMap type="HashMap"
		id="tempid">
		<id property="attaid" column="attaid" />
		<result property="newname" column="newname" />
	</resultMap>
	
	


	<select id="getAllInfo" parameterType="map"
		resultMap="trainingResult">
		SELECT id, type, name, createDate, content ,contcode FROM t_trainingresult
		<where>
			<if test="cname != null">AND name like #{cname}</if>
			<if test="ctype != null">AND type = #{ctype}</if>
		</where>
		 order by id desc limit #{offset} , #{rows}
	</select>

	<select id="getCounts" parameterType="map" resultType="int">
		SELECT count(1) FROM t_trainingresult
		<where>
			<if test="cname != null">AND name like #{cname}</if>
			<if test="ctype != null">AND type = #{ctype}</if>
		</where>
	</select>
	
	<select id="getCounts2" resultType="int">
		SELECT count(1) FROM t_trainingresult
	</select>

	<select id="getInfoById" parameterType="int"
		resultType="trainingResult">
		SELECT id, type, name, createDate, content ,contcode FROM t_trainingresult
		where id=#{id}
	</select>

	<insert id="insert" parameterType="trainingResult">
		INSERT INTO t_trainingresult (type, name,createDate, content,contcode)
		VALUES (#{type}, #{name},#{createDate}, #{content},#{contcode})
	</insert>
	
	<delete id="delete" parameterType="int">
		delete from t_trainingresult where id=#{id}
	</delete>
	
	<update id="update" parameterType="trainingResult">
		update t_trainingresult set type= #{type}, name= #{name},createDate=#{createDate},content=#{content}
		where id=#{id}
	</update>


</mapper>