<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cantodo.content.persistence.PracticeResultMapper">


	<resultMap type="com.cantodo.content.dto.PracticeResult"
		id="practiceresult">
		<id property="id" column="id" />
		<result property="name" column="name" />
		<result property="type" column="type" />
		<result property="createDate" column="createDate" />
		<result property="content" column="content" />
		<result property="contcode" column="contcode" />
	</resultMap>
	

	<select id="getAllInfo" parameterType="map"
		resultMap="practiceresult">
		SELECT id, type, name, createDate, content ,contcode FROM t_practiceresult
		<where>
			<if test="cname != null">AND name like #{cname}</if>
			<if test="ctype != null">AND type = #{ctype}</if>
		</where>
		 order by id desc limit #{offset} , #{rows}
	</select>

	<select id="getCounts" parameterType="map" resultType="int">
		SELECT count(1) FROM t_practiceresult
		<where>
			<if test="cname != null">AND name like #{cname}</if>
			<if test="ctype != null">AND type = #{ctype}</if>
		</where>
	</select>
	
	<select id="getCounts2" resultType="int">
		SELECT count(1) FROM t_practiceresult
	</select>

	<select id="getInfoById" parameterType="int"
		resultType="practiceresult">
		SELECT id, type, name, createDate, content ,contcode FROM t_practiceresult
		where id=#{id}
	</select>

	<insert id="insert" parameterType="practiceresult">
		INSERT INTO t_practiceresult (type, name,createDate, content,contcode)
		VALUES (#{type}, #{name},#{createDate}, #{content},#{contcode})
	</insert>
	
	<delete id="delete" parameterType="int">
		delete from t_practiceresult where id=#{id}
	</delete>
	
	<update id="update" parameterType="practiceresult">
		update t_practiceresult set type= #{type}, name= #{name},createDate=#{createDate},content=#{content}
		where id=#{id}
	</update>


</mapper>